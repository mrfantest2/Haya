# Legacy v2 Docker architecture

v2 used a Node.js agent inside Docker Desktop with WSL 2. It is retained only as historical source lineage.

v3.0.0 is the active production direction and does not require Docker, WSL or Node.js.
